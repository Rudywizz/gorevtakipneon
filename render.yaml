services:
  - type: web
    name: gorevtakip
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: gorevtakipdb
          property: connectionString
      - key: SECRET_KEY
        value: gizli_anahtar
